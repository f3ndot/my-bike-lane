/ = link_to t('.new', :default => t("helpers.links.new")), new_violation_path, :class => 'btn btn-primary'

.violations
  - @violations.each do |violation|
    .row.violation-row
      .span1
        .author
          - if violation.user.nil?
            = image_tag(avatar_url(violation.user, 63), :class => "anonymous-avatar")
          - else
            = link_to image_tag(avatar_url(violation.user, 65)), user_path(violation.user)
      .span11
        .violation-item
          = link_to violation.title, violation_path(violation)
          =l violation.created_at, :format => :short
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_violation_path(violation), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), violation_path(violation), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

